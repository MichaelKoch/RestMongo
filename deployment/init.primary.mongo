use admin

db.createUser(
      {
          user: "admin",
          pwd: "admin",
          roles: [ "root" ]
      }
)

rs.initiate( {
   _id : "DomainProducts",
   members: [
      { _id: 0, host: "localhost:27017" }
   ]
})
